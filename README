// current

> img_update
> world_to_screen

// todo

> matrice de proj / proj iso
> centrage rotation
> cam FPS
> choix gradient

// bugfix
> bug sur rotation en x quand on est quasi en vue de dessus

// misc

> research perror/strerror, errno variable
> retour d'erreur propre/consistent 
>> une fois get input OK, si erreur plus tard ds le programme, utiliser fct
d'erreur qui free la map et return
>> gestion des mallocs propre dans acquisition de l'input et build de la map
> check fonctions interdites (memmove au lieu de ft-memmove etc)

//atexit

> remove debug from functions (check printf)
> remove debug.c from sources and from Makefile !!!
> remove test rule from makefile
> rm fdf_sample from bin/
> remove -g CFLAGS makefile
> remove extra headers (stdlib)
